name: GLOBAL

on:
  push:
  pull_request:

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: TypeScript lint
        run: npm run lint

      - name: Python lint
        run: ruff check .